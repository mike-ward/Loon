<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:m="clr-namespace:Loon.Views"
             xmlns:l="clr-namespace:Loon.Views.Content.Timelines.TweetItem"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="300" d:DesignHeight="450"
             x:Class="Loon.Views.Content.Timelines.TweetItem.TweetItemRelated">

  <Border Margin="0 5"
          Padding="10 0"
          BorderThickness="0.6 0"
          BorderBrush="{DynamicResource ThemeBorderLowBrush}">

    <StackPanel Margin="5 0" Spacing="5">
      <l:TweetItemImage
        Name="TweetItemImage"
        DataContext="{Binding ImageTwitterStatus.ExtendedEntities}"
        IsVisible="{Binding !$parent[m:MainWindow].DataContext.Settings.HideImages}" />

      <TextBlock Classes="normal" Text="{Binding Title, Converter={StaticResource HtmlDecode}}" FontWeight="Bold" />
      <TextBlock Classes="normal" Text="{Binding Description, Converter={StaticResource TrimText}}" />

      <StackPanel Orientation="Horizontal">
        <TextBlock Classes="normal" Text="↗ " />
        <TextBlock Classes="normal" Text="{Binding SiteName}" />
      </StackPanel>

      <l:TweetItemTranslate DataContext="{Binding ImageTwitterStatus}" />
    </StackPanel>
  </Border>
</UserControl>